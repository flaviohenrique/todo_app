{
  "name": "todo_app",
  "packageManager": "yarn@3.0.2",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@yarnpkg/sdks": "^2.4.2-rc.8"
  },
  "scripts": {
    "server": "yarn workspace server",
    "lint": "yarn run prettier '**/*.ts' --write"
  },
  "dependencies": {
    "eslint": "^8.0.0",
    "prettier": "^2.4.1",
    "typescript": "^4.4.4"
  }
}
