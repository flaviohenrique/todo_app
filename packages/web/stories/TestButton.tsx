import React, { VFC } from "react";
import { Button } from "@chakra-ui/react";

const TestButton: VFC<unknown> = () => {
  return <Button colorScheme="teal">Save</Button>;
};

export default TestButton;
